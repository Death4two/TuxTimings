# Kernel module — build separately: https://github.com/amkillam/ryzen_smu
ryzen_smu-src/

# Standalone dev scripts (not used by the app; app uses Linux/scripts/parse_pm_table.py)
dump_pm_table.py
dump_pm_voltages.py

# Windows (ZenTimings) — not part of TuxTimings build
WPF/
WPF-no-themes/
OHWMPlugin/
Common/
ZenStates-Core-src/
